fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# openai >= 1.60 is required for httpx 0.28 compatibility (proxies param fix)
openai>=1.60.0,<3

# PyInstaller is only needed for building the exe (python build.py),
# not required for running the app via uvicorn / launcher.py
pyinstaller>=6.10
